{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build-lamina",
            "type": "shell",
            "command": "g++ main.cpp lexer.cpp parser.cpp interpreter.cpp -o lamina.exe",
            "group": "build",
            "problemMatcher": ["$gcc"],
            "options": {
                "cwd": "${workspaceFolder}/interpreter"
            }
        }
    ]
}
